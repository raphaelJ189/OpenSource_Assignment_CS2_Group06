name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping server
        run: curl -s https://opensource-assignment-cs2-group06.onrender.com/ > /dev/null
